const mdLinks = require('../index');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });

  it('Should return a promise', () => {
    expect(mdLinks()).toBe(typeof 'Promise')
    console.log('FIX ME!');
  });

});
